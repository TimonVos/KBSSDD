<Window x:Class="WpfApp3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp3"
        xmlns:vm="clr-namespace:ViewModel;assembly=ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowState="Maximized">

    <Window.Resources>
        <vm:MainViewModel x:Key="ViewModel"></vm:MainViewModel>
    </Window.Resources>

    <Window.DataContext>
        <Binding Source="{StaticResource ResourceKey=ViewModel}"></Binding>
    </Window.DataContext>

    <StackPanel>
        <ComboBox ItemsSource="{Binding Competences}" SelectedItem="{Binding ActiveCompetence, Mode=TwoWay}"/>
        <TextBlock Text="{Binding ActiveCompetence}"/>
        
    </StackPanel>
    
</Window>
